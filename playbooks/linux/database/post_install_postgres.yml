---
# ============================================================
# Playbook: Post-install PostgreSQL — cria dbs, users e dados de teste
# ============================================================
# Descricao:
#   Executa apos deploy_postgres.yml. Cria 3 databases (app1, app2, awx),
#   4 usuarios de teste e popula tabelas com dados para validacao
#   pos-implementacao e demos.
#
#   Implementacao com ansible.builtin + psql/createdb/createuser — sem
#   dependencia de community.postgresql (nao depende de collection extra
#   no Execution Environment do AAP).
#
# Variaveis Survey AAP:
#   - reset_test_data   : yes/no — apaga e recria dados de teste (default: no)
#   - default_password  : Senha dos usuarios de teste (obrigatorio no survey)
#   - target_hosts      : grupo/pattern do play (default: postgres) — alinhar ao inventario
#
# Job Template AAP: limit padrao "postgres" (grupo no inventario ROQUE).
# ============================================================

- name: Post-install PostgreSQL — criar bancos, usuarios e dados de teste
  hosts: "{{ target_hosts | default('postgres') }}"
  ignore_unreachable: true
  gather_facts: true
  become: true

  vars:
    reset_test_data: "no"
    default_password: ""
    sql_tmp_prefix: "/tmp/aap_post_install"

    # Databases a criar (owner = usuario da aplicacao)
    postgres_databases:
      - name: app1
        owner: app1_user
        comment: "Database da aplicacao de frontend"
      - name: app2
        owner: app2_user
        comment: "Database da aplicacao de catalogo"
      - name: awx
        owner: awx_user
        comment: "Database simulando metadata do AWX"

    # Usuarios de teste
    postgres_test_users:
      - name: app1_user
        comment: "Owner do database app1"
      - name: app2_user
        comment: "Owner do database app2"
      - name: awx_user
        comment: "Owner do database awx"
      - name: readonly_user
        comment: "Usuario de leitura em todos os databases"

    # Schemas (tabelas) por database — idempotente via CREATE TABLE IF NOT EXISTS
    postgres_schemas:
      app1:
        - |
          CREATE TABLE IF NOT EXISTS users (
            id           SERIAL PRIMARY KEY,
            username     VARCHAR(64)  NOT NULL UNIQUE,
            email        VARCHAR(128) NOT NULL,
            full_name    VARCHAR(128) NOT NULL,
            is_active    BOOLEAN      NOT NULL DEFAULT true,
            created_at   TIMESTAMP    NOT NULL DEFAULT NOW()
          )
        - |
          CREATE TABLE IF NOT EXISTS orders (
            id           SERIAL PRIMARY KEY,
            user_id      INTEGER      NOT NULL REFERENCES users(id),
            product      VARCHAR(128) NOT NULL,
            amount       NUMERIC(10,2) NOT NULL,
            status       VARCHAR(32)  NOT NULL DEFAULT 'pending',
            created_at   TIMESTAMP    NOT NULL DEFAULT NOW()
          )
      app2:
        - |
          CREATE TABLE IF NOT EXISTS categories (
            id           SERIAL PRIMARY KEY,
            name         VARCHAR(64)  NOT NULL UNIQUE,
            description  VARCHAR(255)
          )
        - |
          CREATE TABLE IF NOT EXISTS products (
            id           SERIAL PRIMARY KEY,
            name         VARCHAR(128) NOT NULL,
            category_id  INTEGER      REFERENCES categories(id),
            price        NUMERIC(10,2) NOT NULL,
            stock        INTEGER      NOT NULL DEFAULT 0,
            created_at   TIMESTAMP    NOT NULL DEFAULT NOW()
          )
      awx:
        - |
          CREATE TABLE IF NOT EXISTS hosts (
            id           SERIAL PRIMARY KEY,
            hostname     VARCHAR(128) NOT NULL UNIQUE,
            group_name   VARCHAR(64)  NOT NULL,
            last_seen    TIMESTAMP    NOT NULL DEFAULT NOW()
          )
        - |
          CREATE TABLE IF NOT EXISTS jobs (
            id           SERIAL PRIMARY KEY,
            template_name VARCHAR(128) NOT NULL,
            status       VARCHAR(32)  NOT NULL,
            started_at   TIMESTAMP    NOT NULL DEFAULT NOW(),
            finished_at  TIMESTAMP,
            launched_by  VARCHAR(64)  NOT NULL
          )

    # Dados de teste — INSERT ... ON CONFLICT DO NOTHING garante idempotencia
    postgres_seed_data:
      app1:
        - query: >
            INSERT INTO users (username, email, full_name) VALUES
              ('aroque',   'aroque@aroque.com.br',   'Allan Roque'),
              ('jdoe',     'jdoe@aroque.com.br',     'John Doe'),
              ('msilva',   'msilva@aroque.com.br',   'Maria Silva'),
              ('bpereira', 'bpereira@aroque.com.br', 'Bruno Pereira')
            ON CONFLICT (username) DO NOTHING
        - query: >
            INSERT INTO orders (user_id, product, amount, status) VALUES
              (1, 'RHEL Subscription',        1500.00, 'paid'),
              (2, 'Ansible Automation Plat', 25000.00, 'paid'),
              (3, 'OpenShift Cluster',       85000.00, 'pending'),
              (4, 'Red Hat Satellite',        9800.00, 'shipped')
            ON CONFLICT DO NOTHING

      app2:
        - query: >
            INSERT INTO categories (name, description) VALUES
              ('Infra',       'Componentes de infraestrutura'),
              ('Automation',  'Ferramentas de automacao'),
              ('Monitoring',  'Observabilidade e monitoramento')
            ON CONFLICT (name) DO NOTHING
        - query: >
            INSERT INTO products (name, category_id, price, stock) VALUES
              ('Ansible Controller',  2,   0.00,  99),
              ('EDA Controller',      2,   0.00,  99),
              ('Prometheus Server',   3, 100.00,  50),
              ('Grafana Enterprise',  3, 250.00,  30)
            ON CONFLICT DO NOTHING

      awx:
        - query: >
            INSERT INTO hosts (hostname, group_name) VALUES
              ('server01.aroque.com.br', 'apache'),
              ('server02.aroque.com.br', 'postgresql'),
              ('server03.aroque.com.br', 'app')
            ON CONFLICT (hostname) DO NOTHING
        - query: >
            INSERT INTO jobs (template_name, status, finished_at, launched_by) VALUES
              ('Deploy Apache',     'successful', NOW(), 'aroque'),
              ('Deploy PostgreSQL', 'successful', NOW(), 'aroque'),
              ('Deploy App',        'successful', NOW(), 'aroque'),
              ('Disk Cleanup EDA',  'successful', NOW(), 'system')
            ON CONFLICT DO NOTHING

  pre_tasks:
    - name: Validar senha dos usuarios de teste (survey)
      ansible.builtin.assert:
        that:
          - default_password is defined
          - (default_password | string | length) > 0
        fail_msg: "Informe default_password no survey (senha dos usuarios app1_user, app2_user, awx_user, readonly_user)."
      tags:
        - postgres
        - check

  tasks:
    # ── Sanity check ─────────────────────────────────────────────────────────
    - name: Garantir que o servico do PostgreSQL esta em execucao
      ansible.builtin.service:
        name: postgresql
        state: started
        enabled: true
      tags:
        - postgres
        - check

    # ── Usuarios ─────────────────────────────────────────────────────────────
    - name: Criar usuarios de teste (createuser)
      become_user: postgres
      ansible.builtin.command:
        argv:
          - createuser
          - --login
          - --no-superuser
          - --no-createdb
          - --no-createrole
          - "{{ item.name }}"
      register: _createuser
      failed_when:
        - _createuser.rc != 0
        - "'already exists' not in (_createuser.stderr | default(''))"
      changed_when: _createuser.rc == 0
      loop: "{{ postgres_test_users }}"
      loop_control:
        label: "{{ item.name }}"
      tags:
        - postgres
        - users

    - name: Definir senha dos usuarios (ALTER USER)
      become_user: postgres
      ansible.builtin.command:
        argv:
          - psql
          - -v
          - ON_ERROR_STOP=1
          - -c
          - "ALTER USER {{ item.name }} WITH PASSWORD '{{ default_password }}';"
      loop: "{{ postgres_test_users }}"
      loop_control:
        label: "{{ item.name }}"
      changed_when: false
      no_log: true
      tags:
        - postgres
        - users

    # ── Databases ────────────────────────────────────────────────────────────
    - name: Criar databases de teste (createdb -O owner)
      become_user: postgres
      ansible.builtin.command:
        argv:
          - createdb
          - -O
          - "{{ item.owner }}"
          - -E
          - UTF8
          - -T
          - template0
          - "{{ item.name }}"
      register: _createdb
      failed_when:
        - _createdb.rc != 0
        - "'already exists' not in (_createdb.stderr | default(''))"
      changed_when: _createdb.rc == 0
      loop: "{{ postgres_databases }}"
      loop_control:
        label: "{{ item.name }} -> owner={{ item.owner }}"
      tags:
        - postgres
        - databases

    - name: Conceder ALL PRIVILEGES no database ao owner
      become_user: postgres
      ansible.builtin.command:
        argv:
          - psql
          - -v
          - ON_ERROR_STOP=1
          - -c
          - "GRANT ALL PRIVILEGES ON DATABASE {{ item.name }} TO {{ item.owner }};"
      loop: "{{ postgres_databases }}"
      loop_control:
        label: "{{ item.name }} -> {{ item.owner }}"
      changed_when: false
      tags:
        - postgres
        - grants

    - name: Conceder CONNECT no database ao readonly_user
      become_user: postgres
      ansible.builtin.command:
        argv:
          - psql
          - -v
          - ON_ERROR_STOP=1
          - -c
          - "GRANT CONNECT ON DATABASE {{ item.name }} TO readonly_user;"
      loop: "{{ postgres_databases }}"
      loop_control:
        label: "{{ item.name }}"
      changed_when: false
      tags:
        - postgres
        - grants

    # ── Gerar arquivo SQL por database (schema + seed + grants) ──────────────
    - name: Gerar script SQL (schema + seed) por database
      become_user: postgres
      ansible.builtin.copy:
        dest: "{{ sql_tmp_prefix }}_{{ item.key }}.sql"
        owner: postgres
        group: postgres
        mode: "0600"
        content: |
          -- Gerado por AAP post_install_postgres.yml
          SET client_min_messages = WARNING;

          {% if reset_test_data | bool %}
          -- Reset dos dados de teste (reset_test_data=true)
          {% for tbl in ['orders','products','jobs','users','categories','hosts'] %}
          DROP TABLE IF EXISTS {{ tbl }} CASCADE;
          {% endfor %}

          {% endif %}
          -- Schema
          {% for stmt in item.value %}
          {{ stmt }};
          {% endfor %}

          -- Dados de teste
          {% for s in postgres_seed_data[item.key] | default([]) %}
          {{ s.query }};
          {% endfor %}

          -- Grant SELECT ao readonly_user nas tabelas do schema public
          GRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly_user;
          ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO readonly_user;
      loop: "{{ postgres_schemas | dict2items }}"
      loop_control:
        label: "{{ item.key }}"
      tags:
        - postgres
        - schema
        - seed

    - name: Aplicar schema + seed em cada database (psql -f)
      become_user: postgres
      ansible.builtin.command:
        argv:
          - psql
          - -d
          - "{{ item.key }}"
          - -v
          - ON_ERROR_STOP=1
          - -f
          - "{{ sql_tmp_prefix }}_{{ item.key }}.sql"
      register: _apply_sql
      loop: "{{ postgres_schemas | dict2items }}"
      loop_control:
        label: "{{ item.key }}"
      changed_when: >
        ('CREATE TABLE' in (_apply_sql.stdout | default(''))) or
        ('INSERT 0' in (_apply_sql.stdout | default('')) and
         'INSERT 0 0' not in (_apply_sql.stdout | default('')))
      tags:
        - postgres
        - schema
        - seed

    - name: Remover scripts SQL temporarios
      become_user: postgres
      ansible.builtin.file:
        path: "{{ sql_tmp_prefix }}_{{ item.key }}.sql"
        state: absent
      loop: "{{ postgres_schemas | dict2items }}"
      loop_control:
        label: "{{ item.key }}"
      tags:
        - postgres
        - cleanup

    # ── Validacao pos-implementacao (counts) ─────────────────────────────────
    - name: Validar contagem de linhas — app1.users
      become_user: postgres
      ansible.builtin.command:
        argv: [psql, -d, app1, -tAc, "SELECT COUNT(*) FROM users"]
      register: _count_app1_users
      changed_when: false
      tags:
        - postgres
        - validate

    - name: Validar contagem de linhas — app1.orders
      become_user: postgres
      ansible.builtin.command:
        argv: [psql, -d, app1, -tAc, "SELECT COUNT(*) FROM orders"]
      register: _count_app1_orders
      changed_when: false
      tags:
        - postgres
        - validate

    - name: Validar contagem de linhas — app2.categories
      become_user: postgres
      ansible.builtin.command:
        argv: [psql, -d, app2, -tAc, "SELECT COUNT(*) FROM categories"]
      register: _count_app2_cats
      changed_when: false
      tags:
        - postgres
        - validate

    - name: Validar contagem de linhas — app2.products
      become_user: postgres
      ansible.builtin.command:
        argv: [psql, -d, app2, -tAc, "SELECT COUNT(*) FROM products"]
      register: _count_app2_prods
      changed_when: false
      tags:
        - postgres
        - validate

    - name: Validar contagem de linhas — awx.hosts
      become_user: postgres
      ansible.builtin.command:
        argv: [psql, -d, awx, -tAc, "SELECT COUNT(*) FROM hosts"]
      register: _count_awx_hosts
      changed_when: false
      tags:
        - postgres
        - validate

    - name: Validar contagem de linhas — awx.jobs
      become_user: postgres
      ansible.builtin.command:
        argv: [psql, -d, awx, -tAc, "SELECT COUNT(*) FROM jobs"]
      register: _count_awx_jobs
      changed_when: false
      tags:
        - postgres
        - validate

    # ── Relatorio final ──────────────────────────────────────────────────────
    - name: Exibir resumo do post-install
      ansible.builtin.debug:
        msg:
          - "────────────────────────────────────────────────────────"
          - "Post-install PostgreSQL concluido em {{ ansible_hostname }}"
          - "────────────────────────────────────────────────────────"
          - "Databases criados : app1, app2, awx"
          - "Usuarios de teste : app1_user, app2_user, awx_user, readonly_user"
          - "────────────────────────────────────────────────────────"
          - "Contagem de linhas:"
          - "  app1.users       : {{ _count_app1_users.stdout | trim }}"
          - "  app1.orders      : {{ _count_app1_orders.stdout | trim }}"
          - "  app2.categories  : {{ _count_app2_cats.stdout | trim }}"
          - "  app2.products    : {{ _count_app2_prods.stdout | trim }}"
          - "  awx.hosts        : {{ _count_awx_hosts.stdout | trim }}"
          - "  awx.jobs         : {{ _count_awx_jobs.stdout | trim }}"
          - "────────────────────────────────────────────────────────"
      tags:
        - postgres
        - report

  post_tasks:
    - name: Exportar metricas do post-install para workflow (set_stats)
      ansible.builtin.set_stats:
        aggregate: false
        data:
          stats_postgres_host: "{{ ansible_default_ipv4.address }}"
          stats_postgres_databases: "app1,app2,awx"
          stats_postgres_users_created: 4
          stats_postgres_app1_users: "{{ _count_app1_users.stdout | trim }}"
          stats_postgres_app1_orders: "{{ _count_app1_orders.stdout | trim }}"
          stats_postgres_app2_products: "{{ _count_app2_prods.stdout | trim }}"
          stats_postgres_awx_jobs: "{{ _count_awx_jobs.stdout | trim }}"
          stats_phase: "postgres_post_install_ready"
      tags:
        - postgres
        - report
